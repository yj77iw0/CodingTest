```c
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

int* solution(int numbers[], size_t numbers_len, int num1, int num2) {
    int* answer = (int*)malloc(sizeof(int) * numbers_len); // 배열의 길이만큼 동적 메모리 할당.

    int count = 0;

    for (int i = num1; i <= num2; i++) { // num1 부터 num2 까지 잘라 출력해야하기 때문.
        answer[i - num1] = numbers[i];
    }

    return answer;
}
```
